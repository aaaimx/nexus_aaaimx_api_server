### Clubs API - Nexus AAAIMX

### Variables
@baseUrl = http://localhost:8000/api/v1

# ------------------------------------------------------------
### GET /clubs
# Description: Get all clubs catalog (public access)
GET {{baseUrl}}/clubs
Content-Type: application/json

# ------------------------------------------------------------
### POST /clubs
# Description: Create a new club (committee and president only)
# Note: Requires authentication and proper role permissions
POST {{baseUrl}}/clubs
Content-Type: application/json

{
  "name": "Club de Inteligencia Artificial",
  "description": "Club dedicado al desarrollo y estudio de la inteligencia artificial",
  "logoUrl": "https://example.com/ai-club-logo.png"
}

# ------------------------------------------------------------
### PUT /clubs/:id
# Description: Update an existing club (committee and president only)
# Note: Replace {club-id} with actual club UUID
PUT {{baseUrl}}/clubs/{club-id}
Content-Type: application/json

{
  "name": "Club de IA y Machine Learning",
  "description": "Club dedicado al desarrollo y estudio de la inteligencia artificial y machine learning",
  "logoUrl": "https://example.com/ai-ml-club-logo.png"
}

# ------------------------------------------------------------
### DELETE /clubs/:id
# Description: Delete a club (committee and president only)
# Note: Replace {club-id} with actual club UUID
DELETE {{baseUrl}}/clubs/{club-id}
Content-Type: application/json

# ============================================================
### CLUB REQUESTS MANAGEMENT (Leaders & Co-Leaders Only)
# ============================================================

# ------------------------------------------------------------
### GET /clubs/:id/requests
# Description: List club requests for a specific club (leaders and co-leaders only)
# Note: Replace {club-id} with actual club UUID
GET {{baseUrl}}/clubs/{club-id}/requests
Content-Type: application/json

# ------------------------------------------------------------
### GET /clubs/requests
# Description: List all club requests for user's clubs (leaders and co-leaders only)
GET {{baseUrl}}/clubs/requests
Content-Type: application/json

# ------------------------------------------------------------
### POST /clubs/requests/:requestId/approve
# Description: Approve a club request (leaders and co-leaders only)
# Note: Replace {request-id} with actual request UUID
POST {{baseUrl}}/clubs/requests/{request-id}/approve
Content-Type: application/json

{
  "adminMessage": "Welcome to the club! We're excited to have you join us."
}

# ------------------------------------------------------------
### POST /clubs/requests/:requestId/reject
# Description: Reject a club request (leaders and co-leaders only)  
# Note: Replace {request-id} with actual request UUID
POST {{baseUrl}}/clubs/requests/{request-id}/reject
Content-Type: application/json

{
  "adminMessage": "Thank you for your interest. We encourage you to reapply in the future."
}